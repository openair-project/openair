% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities.R
\name{datePad}
\alias{datePad}
\title{Pad a time-series dataframe and optionally fill values by block}
\usage{
datePad(
  mydata,
  type = NULL,
  interval = NULL,
  start.date = NULL,
  end.date = NULL,
  fill = FALSE,
  print.int = FALSE,
  ...
)
}
\arguments{
\item{mydata}{Data.frame or tibble containing at least a 'date' column (Date
or POSIXt).}

\item{type}{\code{NULL} or character vector of column names to group by.}

\item{interval}{\code{NULL} or character string describing target interval (e.g.
"1 min", "1 hour"). If \code{NULL}, the native interval is used.}

\item{start.date}{Optional start date/time. If \code{NULL}, the group's minimum
date is used.}

\item{end.date}{Optional end date/time. If \code{NULL}, the group's maximum date
is used.}

\item{fill}{Logical; when \code{TRUE} performs block-based filling described
above. When \code{FALSE} just completes the sequence leaving \code{NA} values.}

\item{print.int}{Logical; when \code{TRUE} prints detected/selected interval
messages.}

\item{...}{Passed to \code{\link[=cutData]{cutData()}}.}
}
\value{
A dataframe expanded to the requested sequence with values filled
according to 'fill'. The returned object preserves the 'date' column type
and timezone (for POSIXt).
}
\description{
Expand a dataframe that contains a 'date' column to a regular sequence of
timestamps between specified start and end dates. The function can operate in
two modes:
\itemize{
\item \code{fill = FALSE}: simply complete the sequence at the target interval.
\item \code{fill = TRUE}: regularise the data at the native interval to create
explicit blocks, then expand to the target interval and carry the block's
values forward so that intra-block timestamps inherit the block's measured
value (block-filling behaviour).
}
}
\details{
The function detects the native input interval automatically if \code{interval} is
not supplied, supports grouping via \code{type}, and preserves timezones for
POSIXt date columns.
}
\examples{
df <- mydata[-c(2, 4, 7), ] # Remove some rows to create gaps
datePad(df)

}
