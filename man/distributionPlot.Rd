% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributionPlot.R
\name{distributionPlot}
\alias{distributionPlot}
\title{Plot the distribution of a variable with conditioning}
\usage{
distributionPlot(
  mydata,
  pollutant = "nox",
  method = c("histogram", "freqpoly", "density", "ecdf"),
  binwidth = NULL,
  bins = 30,
  position = NULL,
  log = FALSE,
  group = "default",
  type = "default",
  cols = "hue",
  theme = "default",
  key.title = group,
  key.position = "top",
  ref.x = NULL,
  ref.y = NULL,
  auto.text = TRUE,
  plot = TRUE,
  ...
)
}
\arguments{
\item{mydata}{A data frame.}

\item{pollutant}{Name of the pollutant(s) to plot contained in \code{mydata}.}

\item{method}{One of: \code{"histogram"}, \code{"freqpoly"}, \code{"density"}, or \code{"ecdf"}.
Note that \code{"freqpoly"} is effectively a line chart equivalent of a
histogram, and may appear less cluttered with many groups.}

\item{binwidth, bins}{Used when \code{method = "histogram"} or \code{"freqpoly"}.
\code{binwidth} sets the width of the bins. \code{bins} sets the number of bins,
defaulting to \code{30}. \code{bins} is overridden by \code{binwidth}.}

\item{position}{A string representing a \code{ggplot2} "position" - see
\code{\link[ggplot2:position_identity]{ggplot2::position_identity()}} and similar functions. When \code{NULL}, will use
\code{"stack"} for histograms and \code{"identity"} for other methods. Also useful is
\code{"fill"} in conjunction with the \code{group} argument which will 'normalise'
the y-axis to show a percentage rather than an absolute count or density
estimate. Not used when \code{method = "ecdf"}, which must be \code{"identity"}. Note
that density functions will use 'count' over 'density' for non-identity
\code{position}s.}

\item{log}{Should the x-axis appear on a log scale? The default is \code{FALSE}.
If \code{TRUE} a well-formatted log10 scale is used.}

\item{group}{This sets the grouping variable to be used. For example, if a
data frame had a column \code{site} setting \code{group = "site"} will plot all sites
together in each panel. Passed to \code{\link[=cutData]{cutData()}}.}

\item{type}{Character string(s) defining how data should be split/conditioned
before plotting. \code{"default"} produces a single panel using the entire
dataset. Any other options will split the plot into different panels - a
roughly square grid of panels if one \code{type} is given, or a 2D matrix of
panels if two \code{types} are given. \code{type} is always passed to \code{\link[=cutData]{cutData()}},
and can therefore be any of:
\itemize{
\item A built-in type defined in \code{\link[=cutData]{cutData()}} (e.g., \code{"season"}, \code{"year"},
\code{"weekday"}, etc.). For example, \code{type = "season"} will split the plot into
four panels, one for each season.
\item The name of a numeric column in \code{mydata}, which will be split into
\code{n.levels} quantiles (defaulting to 4).
\item The name of a character or factor column in \code{mydata}, which will be used
as-is. Commonly this could be a variable like \code{"site"} to ensure data from
different monitoring sites are handled and presented separately. It could
equally be any arbitrary column created by the user (e.g., whether a nearby
possible pollutant source is active or not).
}

Most \code{openair} plotting functions can take two \code{type} arguments. If two are
given, the first is used for the columns and the second for the rows.}

\item{cols}{Colours to use for plotting. Can be a pre-set palette (e.g.,
\code{"turbo"}, \code{"viridis"}, \code{"tol"}, \code{"Dark2"}, etc.) or a user-defined vector
of R colours (e.g., \code{c("yellow", "green", "blue", "black")} - see
\code{\link[=colours]{colours()}} for a full list) or hex-codes (e.g., \code{c("#30123B", "#9CF649", "#7A0403")}). Alternatively, can be a list of arguments to control the
colour palette more closely (e.g., \code{palette}, \code{direction}, \code{alpha}, etc.).
See \code{\link[=openColours]{openColours()}} and \code{\link[=colourOpts]{colourOpts()}} for more details.}

\item{theme}{A string representing an overall plot theme, defaulting to
\code{"default"}. This option makes sweeping changes to non-data plot features
such as fonts, colours, line widths, and so on, and may also change default
arguments like \code{cols} if not set by the user. Can also take a
\code{\link[ggplot2:theme]{ggplot2::theme()}} object, which will be used to modify the \code{"default"}
theme. Pre-set options include:
\itemize{
\item \code{"default"}, a lattice-inspired theme resembling the traditional
\code{openair} look, with structured panels and visible gridlines.
\item \code{"dark"}, a dark-background variant of the default theme, designed for
presentations and low-light viewing, using high-contrast text and colour
palettes optimised for visibility against dark panels.
\item \code{"modern"}, a minimalist, contemporary theme inspired by tools such as
Plotly and Observable Plot, with reduced visual clutter, horizontal
emphasis in gridlines, a clean legend style, and typography suited to
dashboards and reports.
\item \code{"soft"}, a low-contrast, 'editorial' theme with warm
background tones, subtle gridlines, and gently desaturated colours,
designed for reports and publication-style figures, particularly where a
calmer appearance improves readability.
\item \code{"print"}, a strictly greyscale theme optimised for black-and-white
reproduction, with stronger structural elements such as clearer gridlines
and axis definitions to ensure good contrast and readability in printed or
photocopied outputs.
}

Please note that if a global theme is set with \code{\link[ggplot2:theme_set]{ggplot2::theme_set()}} to
anything other than the default \code{\link[ggplot2:theme_grey]{ggplot2::theme_grey()}}, the selected
openair theme will not be fully applied; instead, only minimal adjustments
(such as legend positioning) will be made.}

\item{key.title}{Used to set the title of the legend. The legend title is
passed to \code{\link[=quickText]{quickText()}} if \code{auto.text = TRUE}.}

\item{key.position}{Location where the legend is to be placed. Allowed
arguments include \code{"top"}, \code{"right"}, \code{"bottom"}, \code{"left"} and \code{"none"},
the last of which removes the legend entirely.}

\item{ref.x}{Either a single value or values representing the x axis
intercepts to draw lines, or a list such as that provided by \code{\link[=refOpts]{refOpts()}} to
customise the colour/width/type/etc. of each line. See \code{\link[=refOpts]{refOpts()}} for more
details.}

\item{ref.y}{Either a single value or values representing the y axis
intercepts to draw lines, or a list such as that provided by \code{\link[=refOpts]{refOpts()}} to
customise the colour/width/type/etc. of each line. See \code{\link[=refOpts]{refOpts()}} for more
details.}

\item{auto.text}{Either \code{TRUE} (default) or \code{FALSE}. If \code{TRUE} titles and
axis labels will automatically try and format pollutant names and units
properly, e.g., by subscripting the "2" in "NO2". Passed to \code{\link[=quickText]{quickText()}}.}

\item{plot}{When \code{openair} plots are created they are automatically printed
to the active graphics device. \code{plot = FALSE} deactivates this behaviour.
This may be useful when the plot \emph{data} is of more interest, or the plot is
required to appear later (e.g., later in a Quarto document, or to be saved
to a file).}

\item{...}{Addition options are passed on to \code{\link[=cutData]{cutData()}} for \code{type} handling.
Some additional arguments are also available, varying somewhat in different
plotting functions:
\itemize{
\item \code{title}, \code{subtitle}, \code{caption}, \code{tag}, \code{xlab} and \code{ylab} control the plot
title, subtitle, caption, tag, x-axis label and y-axis label, passed to
\code{\link[ggplot2:labs]{ggplot2::labs()}} via \code{\link[=quickText]{quickText()}} if \code{auto.text = TRUE}.
\item \code{xlim}, \code{ylim} and \code{limits} control the limits of the x-axis, y-axis and
colorbar scales.
\item \code{ncol} and \code{nrow} set the number of columns and rows in a faceted plot.
\item \code{scales} can be \code{"fixed"}, \code{"free_x"}, \code{"free_y"} or \code{"free"} to control
whether axes are shared across facets when using \code{type}. Also supported are
the legacy \code{x.relation} and \code{y.relation}, which can be either \code{"same"} or
\code{"free"} and get remapped to \code{scales} automatically.
\item Similarly, \code{space}, \code{axes}, \code{axis.labels}, \code{switch} and \code{strip.position}
can be used to customise the appearance of faceted plots. See
\code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}} and \code{\link[ggplot2:facet_grid]{ggplot2::facet_grid()}} for the arguments these
take.
\item \code{fontsize} overrides the overall font size of the plot by setting the
\code{text} argument of \code{\link[ggplot2:theme]{ggplot2::theme()}}. It may also be applied
proportionately to any \code{openair} annotations (e.g., N/E/S/W labels on polar
coordinate plots).
\item Various graphical parameters are also supported: \code{linewidth},
\code{linetype},\code{ shape}, \code{size}, \code{border}, and \code{alpha}. Not all parameters
apply to all plots. These can take a single value, or a vector of multiple
values - e.g., \code{shape = c(1, 2)} - which will be recycled to the length of
values needed.
\item \code{lineend}, \code{linejoin} and \code{linemitre} tweak the appearance of line
plots; see \code{\link[ggplot2:geom_line]{ggplot2::geom_line()}} for more information.
\item In polar coordinate plots, \code{annotate = FALSE} will remove the N/E/S/W
labels and any other annotations.
}}
}
\value{
an \link[=openair-package]{openair} object
}
\description{
This function plots the distribution of one or more pollutants or other
variables as a histogram, kernel density estimate function, or empirical
cumulative distribution function. The choice of method comes with trade-offs;
for example, histograms are often more easily interpretable but can easily
get cluttered and are heavily dependent on bin width, whereas density
functions appear 'cleaner' with many overlapping groups, but can be more
challenging to interpret.
}
\examples{
distributionPlot(mydata, pollutant = "no2", group = "season")

\dontrun{
distributionPlot(
  mydata,
  pollutant = "no2",
  group = "weekend",
  method = "density",
  cols = "tol"
)

distributionPlot(
  mydata,
  pollutant = "no2",
  group = "wd",
  position = "fill",
  wd.res = 4,
  alpha = 0.75,
  cols = "tol"
)
}
}
\author{
Jack Davison
}
